# 湖南大学人脸识别考勤系统——完整功能测试流程清单

> 适用版本：湖南大学主题版（2026-09-09）  
> 测试范围：Web 前端、Android App、Java 后端、人脸识别服务  
> 建议执行顺序：环境检查 → 公共功能 → 教师准备数据 → 学生人脸与签到 → 请假 → 统计与导出 → 权限及异常 → Android

## 一、测试记录

| 项目 | 填写内容 |
|---|---|
| 测试人员 |  |
| 测试日期 |  |
| 测试版本 |  |
| 浏览器及版本 |  |
| Android 机型及系统 |  |
| 后端地址 | `http://127.0.0.1:8080/api` |
| 人脸服务地址 | `http://127.0.0.1:5000` |
| 测试结论 | □ 通过　□ 有条件通过　□ 不通过 |

状态填写建议：`通过 / 失败 / 阻塞 / 不适用`。失败时记录截图、账号、时间、课程和操作步骤。

## 二、测试账号与数据

| 角色 | 默认账号 | 默认密码 | 用途 |
|---|---|---|---|
| 管理员 | `admin` | `admin123` | 管理员权限与全局数据验证 |
| 教师 | `teacher1` | `password123` | 课程、签到任务、审批、统计 |
| 教师 | `teacher2` | `password123` | 越权验证 |
| 学生 | `student1` | `password123` | 本人签到、组长、请假 |
| 学生 | `student2` | `password123` | 小组成员、非本人及重复操作验证 |

建议另注册一个临时学生账号用于修改密码测试，避免修改公共测试账号。测试前确认 `student1`、`student2` 已被安排到需要测试的课程；学生不能自行选课或退课。

## 三、环境与启动检查

| 编号 | 测试步骤 | 预期结果 | 状态 |
|---|---|---|---|
| ENV-01 | 启动人脸服务，访问 `http://127.0.0.1:5000` | 返回服务正常状态，模型加载成功 | □ |
| ENV-02 | 启动 Java 后端，访问后端接口 | 8080 端口可访问，无启动异常 | □ |
| ENV-03 | 启动前端，访问 `http://127.0.0.1:5173` | 自动进入登录页，页面资源完整 | □ |
| ENV-04 | 查看三个服务日志 | 无持续报错、端口冲突或数据库建表失败 | □ |
| ENV-05 | 首次启动本地模式 | 自动创建 H2 数据库和默认测试数据，无需 MySQL | □ |
| ENV-06 | 停止人脸服务后打开签到页 | 页面给出可理解的服务不可用提示，不假成功 | □ |
| ENV-07 | 恢复人脸服务并重试 | 无需刷新整个系统即可重新检测 | □ |
| ENV-08 | 停止后端后执行查询 | 页面提示网络或服务器异常，不清空已有界面数据 | □ |

## 四、主题、布局与通用交互

| 编号 | 测试步骤 | 预期结果 | 状态 |
|---|---|---|---|
| UI-01 | 打开登录页 | 显示湖南大学校徽校名、湖大红主色和系统名称 | □ |
| UI-02 | 打开注册页 | 校徽清晰，无拉伸、截断或模糊；主按钮为湖大红 | □ |
| UI-03 | 登录后查看顶部栏、侧栏和仪表盘 | 品牌色统一，校徽位置合理，文字对比度清晰 | □ |
| UI-04 | 检查成功、迟到、缺勤、请假标签 | 成功保持绿色、迟到黄色、缺勤红色、请假灰蓝色 | □ |
| UI-05 | 浏览器宽度分别调整到 1440、1024、768 像素 | 页面不横向溢出，表格与导航可正常使用 | □ |
| UI-06 | 手机竖屏约 390×844 检查登录、首页、课程、签到 | 底部导航完整，按钮可点击，摄像头区域不超屏 | □ |
| UI-07 | 手机横竖屏切换 | 内容自动重排，无白屏、遮挡和按钮丢失 | □ |
| UI-08 | 快速连续点击提交类按钮 | 加载期间按钮受控，不产生多条重复数据 | □ |
| UI-09 | 访问不存在的地址 | 显示 404 页面，返回按钮可用且采用当前主题色 | □ |

## 五、服务器设置、注册、登录与会话

| 编号 | 测试步骤 | 预期结果 | 状态 |
|---|---|---|---|
| AUTH-01 | 登录页不填写账号密码直接点“登录” | 显示必填校验，不发送登录请求 | □ |
| AUTH-02 | 输入错误用户名或密码 | 提示账号或密码错误，不进入系统 | □ |
| AUTH-03 | 使用 `student1` 登录 | 登录成功并进入学生仪表盘 | □ |
| AUTH-04 | 分别使用教师、管理员账号登录 | 登录成功，菜单与角色对应 | □ |
| AUTH-05 | 展开“服务器设置”，填写不带 `http://` 或 `https://` 的地址 | 阻止保存并提示地址格式要求 | □ |
| AUTH-06 | 保存有效服务器地址后刷新页面 | 配置被保留，登录请求使用新地址 | □ |
| AUTH-07 | 清空服务器地址并保存 | 恢复网页默认 `/api` 地址 | □ |
| AUTH-08 | 注册页输入少于 4 位或含非法字符的用户名 | 前端阻止提交并显示规则 | □ |
| AUTH-09 | 输入不含大小写字母和数字组合的弱密码 | 前端阻止提交 | □ |
| AUTH-10 | 两次密码不一致 | 显示“两次输入的密码不一致” | □ |
| AUTH-11 | 学号为空或含非法字符 | 前端阻止提交 | □ |
| AUTH-12 | 使用全新用户名和学号注册学生 | 注册成功并跳转登录页；角色固定为学生 | □ |
| AUTH-13 | 用重复用户名或重复学号注册 | 返回明确冲突提示，不创建重复账号 | □ |
| AUTH-14 | 登录后刷新页面 | 登录状态与用户信息保留 | □ |
| AUTH-15 | 退出登录并尝试浏览器后退 | 不能重新进入受保护页面 | □ |
| AUTH-16 | 未登录直接访问 `/courses` 或 `/attendance` | 自动跳转登录页并携带原目标地址 | □ |
| AUTH-17 | 使用过期或无效令牌访问系统 | 清理失效会话并返回登录页 | □ |
| AUTH-18 | 打开登录页检查账号入口 | 仅允许登录，提示账号由管理员维护，无自行注册入口 | □ |

## 六、个人信息与修改密码

| 编号 | 测试步骤 | 预期结果 | 状态 |
|---|---|---|---|
| USER-01 | 打开“个人信息” | 用户名、姓名、学号、角色及状态正确，所有角色均无编辑入口 | □ |
| USER-02 | 学生检查个人信息和菜单 | 不出现人脸注册、删除或重新录入入口 | □ |
| USER-03 | 修改密码时输入错误原密码 | 修改失败并提示原密码错误 | □ |
| USER-04 | 新密码不足 8 位或缺少大小写字母、数字 | 前端或后端拒绝 | □ |
| USER-05 | 新密码与原密码相同 | 后端拒绝并提示不能与原密码相同 | □ |
| USER-06 | 两次新密码不一致 | 前端阻止提交 | □ |
| USER-07 | 使用临时账号正确修改密码 | 提示成功、清除会话并返回登录页 | □ |
| USER-08 | 分别用旧密码和新密码登录 | 旧密码失败，新密码成功 | □ |

## 七、管理员课程管理、教师教学管理与学生课程查看

| 编号 | 测试步骤 | 预期结果 | 状态 |
|---|---|---|---|
| CRS-01 | 教师打开“课程管理” | 只显示本人课程及课程人数、时间和状态，不显示新增、编辑和停用入口 | □ |
| CRS-02 | 管理员创建课程时留空课程名称、代码或任课教师 | 阻止保存并显示必填提示 | □ |
| CRS-03 | 管理员创建一门测试课程并指定教师 | 保存成功，管理员列表与该教师课程列表立即出现新课程 | □ |
| CRS-04 | 创建重复课程代码 | 明确提示冲突，不生成重复课程 | □ |
| CRS-05 | 管理员编辑课程名称、教师、教室、星期、时段、学期和容量 | 保存后列表及详情同步更新 | □ |
| CRS-06 | 将结束时间设置为无效值 | 阻止保存或返回明确校验提示 | □ |
| CRS-07 | 点击“查看”进入课程详情 | 课程代码、教师、时间、教室、学期、容量、人数正确 | □ |
| CRS-08 | 教师按学号向本人课程添加学生并设置班级 | 名单和班级保存成功，重复添加被拒绝 | □ |
| CRS-09 | 管理员停用课程并确认 | 状态改为“已停用”，学生不可用于签到 | □ |
| CRS-10 | 取消停用确认框 | 课程状态不变 | □ |
| CRS-11 | 学生打开“我的课程” | 仅显示学校已统一安排的课程 | □ |
| CRS-12 | 检查学生课程页 | 不出现“选课”“退选”“退课”等入口或文案 | □ |
| CRS-13 | 管理员新建但未安排课程的学生查看课程 | 显示空状态，不显示其他学生课程 | □ |
| CRS-14 | 教师尝试修改或停用课程 | 前端无入口，直接调用接口返回 403 | □ |

## 八、人脸采集服务专项（学生端无入口）

> 人脸属于敏感个人信息。系统使用学校预先采集的人脸资料，网页和 App 均不提供注册或删除入口。

| 编号 | 测试步骤 | 预期结果 | 状态 |
|---|---|---|---|
| FACE-01 | 检查学生侧栏和个人信息 | 不出现人脸注册、重新录入或删除入口 | □ |
| FACE-02 | 手工访问原人脸注册地址 | 显示页面不存在，不进入采集页 | □ |
| FACE-03 | 未登录调用账号或人脸注册接口 | 请求被拒绝，不写入用户或人脸资料 | □ |
| FACE-04 | 使用已预先采集人脸的学生进行签到 | 可进入快速或活体检测并完成身份核验 | □ |

## 九、教师发起限时签到

> 建议使用已有学生名单的课程，并把课程星期和时间临时调整为当前日期及时段。

| 编号 | 测试步骤 | 预期结果 | 状态 |
|---|---|---|---|
| SES-01 | 教师进入课程详情查看“限时签到” | 未发起时显示“尚未发起” | □ |
| SES-02 | 选择 5、10、15 分钟任一预设并发起 | 创建成功，显示服务端截止时间和倒计时 | □ |
| SES-03 | 选择自定义时长，输入 1 分钟 | 可正常发起 | □ |
| SES-04 | 自定义输入 0、241 或越界数值 | 控件限制在 1–240 分钟 | □ |
| SES-05 | 启用迟到阈值并设置小于总时长的分钟数 | 发起成功，迟到规则被保存 | □ |
| SES-06 | 迟到阈值等于或超过总时长 | 控件限制或后端拒绝 | □ |
| SES-07 | 签到进行中刷新页面或重新登录 | 截止时间不重置，继续显示剩余时间 | □ |
| SES-08 | 同一课程同一天重复发起 | 被拒绝，不创建第二个任务 | □ |
| SES-09 | 签到进行中查看教师首页 | 显示课程、已签到人数/应签到人数和进度 | □ |
| SES-10 | 任务截止后刷新 | 状态变为已结束，未签到学生按规则结算 | □ |
| SES-11 | 已批准请假的学生在截止结算后检查状态 | 记录为请假而不是缺勤 | □ |
| SES-12 | 停用课程后尝试发起签到 | 被拒绝或不提供有效发起入口 | □ |

## 十、学生快速检测与活体签到

| 编号 | 测试步骤 | 预期结果 | 状态 |
|---|---|---|---|
| SIGN-01 | 从首页当前签到卡点击“立即签到” | 自动带入当前课程，无需再次选课 | □ |
| SIGN-02 | 未开启摄像头时查看签到按钮 | 签到按钮不可用 | □ |
| SIGN-03 | 没有预采集人脸资料的学生尝试签到 | 阻止签到并提示联系管理人员核对资料 | □ |
| SIGN-04 | 选择当前没有有效签到任务的课程 | 提示签到未开始、已结束或不在有效时段 | □ |
| SIGN-05 | 开启摄像头但画面无人脸 | 提示将脸移入框内或未检测到人脸 | □ |
| SIGN-06 | 选择“快速检测”并完成人脸核验 | 不要求眨眼或转头，身份匹配后完成签到 | □ |
| SIGN-07 | 选择“活体检测”，真人正视镜头 | 先进入眨眼检测阶段 | □ |
| SIGN-08 | 完整完成“睁眼—闭眼—重新睁眼” | 眨眼被识别，短暂动作不被漏检 | □ |
| SIGN-09 | 眨眼完成后按提示左右转头 | 分阶段检测通过并进入身份核验 | □ |
| SIGN-10 | 中途换成另一张脸 | 身份一致性核验失败，不允许签到 | □ |
| SIGN-11 | 使用未匹配的人脸完成动作 | 提示未找到匹配用户 | □ |
| SIGN-12 | 本人在任一模式完成核验 | 显示姓名、学号、签到时间及“签到成功” | □ |
| SIGN-13 | 在迟到阈值之后、截止之前签到 | 显示“签到成功（迟到）”，记录为迟到 | □ |
| SIGN-14 | 签到成功后等待 3 秒 | 自动跳转考勤记录页 | □ |
| SIGN-15 | 同一学生对同一课程当天重复签到 | 被拒绝，不生成重复记录 | □ |
| SIGN-16 | 身份核验凭据超过 2 分钟再提交 | 凭据失效，要求重新检测 | □ |
| SIGN-17 | 尝试重复使用同一身份核验凭据 | 第二次被拒绝 | □ |
| SIGN-18 | 检测失败后点击“重新检测” | 清理错误状态并可重新开始 | □ |

## 十一、学生小组与小组快速签到

| 编号 | 测试步骤 | 预期结果 | 状态 |
|---|---|---|---|
| GRP-01 | 未加入小组的 `student1` 创建小组 | 创建成功，自己成为组长和首位成员 | □ |
| GRP-02 | 小组名称为空时创建 | 阻止创建并提示填写名称 | □ |
| GRP-03 | 已在小组的学生再次创建小组 | 被拒绝 | □ |
| GRP-04 | 组长按学号添加 `student2` | 添加成功，列表显示姓名、学号及人脸状态 | □ |
| GRP-05 | 添加不存在的学号 | 提示未找到学生 | □ |
| GRP-06 | 添加已经在其他小组的学生 | 被拒绝，不产生重复成员关系 | □ |
| GRP-07 | 普通成员查看小组 | 能查看成员，但没有添加和移除按钮 | □ |
| GRP-08 | 组长点击移除成员后取消确认 | 成员仍保留 | □ |
| GRP-09 | 组长确认移除普通成员 | 成员从列表移除 | □ |
| GRP-10 | 尝试移除组长本人 | 不提供入口或后端拒绝 | □ |
| GRP-11 | 组长进入签到页开启“小组快速检测” | 显示本组成员勾选列表 | □ |
| GRP-12 | 不勾选任何成员 | “检测下一位成员”不可用 | □ |
| GRP-13 | 勾选部分成员并开始 | 只处理被勾选人员 | □ |
| GRP-14 | 每位成员分别完成人脸、眨眼和转头 | 每人独立核验，不允许跳过活体 | □ |
| GRP-15 | 识别到非本组成员 | 拒绝代签并提示身份不符 | □ |
| GRP-16 | 成员完成后继续检测下一位 | 已完成成员标记为“已签到”，流程继续 | □ |
| GRP-17 | 为已经签到的成员重复操作 | 不生成重复考勤记录 | □ |
| GRP-18 | 普通成员尝试替小组其他人签到 | 被拒绝，只允许组长管理本组签到 | □ |

## 十二、考勤记录

| 编号 | 测试步骤 | 预期结果 | 状态 |
|---|---|---|---|
| REC-01 | 学生打开“考勤记录” | 只显示本人记录 | □ |
| REC-02 | 教师打开“考勤记录” | 只显示本人所授课程相关记录 | □ |
| REC-03 | 管理员登录后检查菜单 | 不出现考勤记录、考勤统计或请假审批入口 | □ |
| REC-04 | 教师检查记录字段 | 每次课列出全部学生，课程、姓名、学号、日期、时段、状态和签到时间正确 | □ |
| REC-05 | 检查正常、迟到、缺勤、请假记录 | 状态文字和颜色正确 | □ |
| REC-06 | 点击“刷新” | 获取最新记录，不重复追加旧数据 | □ |
| REC-07 | 切换每页 10、20、50、100 条并翻页 | 总数、页码和列表一致 | □ |
| REC-08 | 无记录账号打开页面 | 显示空表或空状态，无报错 | □ |
| REC-09 | 教师对特殊情况学生选择“修改状态” | 可改为正常、迟到、缺勤或请假，刷新后保持一致 | □ |

## 十三、请假申请与审批

| 编号 | 测试步骤 | 预期结果 | 状态 |
|---|---|---|---|
| LEAVE-01 | 学生不选课程、日期或原因直接提交 | 阻止提交并提示缺失项 | □ |
| LEAVE-02 | 原因超过 500 字 | 输入受限或后端拒绝 | □ |
| LEAVE-03 | 学生选择本人课程、日期并填写原因 | 申请成功，状态为“待审批” | □ |
| LEAVE-04 | 查看请假详情 | 课程、学生、时段、时长、原因、申请时间和状态完整 | □ |
| LEAVE-05 | 教师查看请假列表 | 只显示本人课程的申请 | □ |
| LEAVE-06 | 教师批准申请 | 状态变为“已批准”，记录审批人、时间和意见 | □ |
| LEAVE-07 | 批准后检查对应考勤 | 生成或更新为“请假”状态 | □ |
| LEAVE-08 | 教师驳回时不填写原因 | 阻止驳回并要求填写原因 | □ |
| LEAVE-09 | 填写原因后驳回 | 状态变为“已驳回”，学生端可看到意见 | □ |
| LEAVE-10 | 对已经审批的申请重复审批 | 被拒绝，原审批结果不变 | □ |
| LEAVE-11 | `teacher2` 审批 `teacher1` 课程的申请 | 被拒绝 | □ |
| LEAVE-12 | 学生已经真实签到后再批准同日请假 | 拒绝覆盖真实签到，提示先处理考勤记录 | □ |

## 十四、统计分析与 Excel 导出

| 编号 | 测试步骤 | 预期结果 | 状态 |
|---|---|---|---|
| STAT-01 | 教师打开“考勤统计”并选择课程 | 显示数据卡、状态饼图、排名柱状图、趋势图和明细表 | □ |
| STAT-02 | 切换班级筛选 | 只显示对应班级学生，统计同步变化 | □ |
| STAT-03 | 输入姓名或学号查询 | 结果匹配关键字 | □ |
| STAT-04 | 选择日期范围 | 只统计范围内已结束的签到任务 | □ |
| STAT-05 | 切换日、周、月趋势粒度 | 横轴与聚合结果正确 | □ |
| STAT-06 | 按姓名、学号、正常、迟到、请假、缺勤、出勤率排序 | 排序结果正确且稳定 | □ |
| STAT-07 | 查看学生考勤详情 | 姓名、学号、班级、范围、各状态次数及出勤率正确 | □ |
| STAT-08 | 检查出勤率公式 | 等于（正常＋迟到）÷（正常＋迟到＋缺勤）；请假不进入分母 | □ |
| STAT-09 | 有效考勤次数为零的学生 | 出勤率显示“—”，不显示 0% 或异常数值 | □ |
| STAT-10 | 尚未结束的任务 | 不进入统计和导出 | □ |
| STAT-11 | 使用异常条件筛选 | 低出勤率或有缺勤的学生及异常原因正确 | □ |
| STAT-12 | 修改筛选条件但未重新查询 | 导出按钮受控，避免导出旧条件 | □ |
| STAT-13 | 点击“导出 Excel” | 下载真正的 `.xlsx` 文件，文件可由 Excel 正常打开 | □ |
| STAT-14 | 核对导出内容 | 日期、课程、班级、姓名、学号、签到时间、状态和请假信息齐全 | □ |
| STAT-15 | 核对导出范围 | 与页面已应用的课程、班级、学生和日期条件一致 | □ |
| STAT-16 | 无可导出数据时查看按钮 | 导出按钮禁用，不下载空的错误文件 | □ |

## 十五、仪表盘数据联动

| 编号 | 测试步骤 | 预期结果 | 状态 |
|---|---|---|---|
| DASH-01 | 学生登录查看首页 | 今日课程、迟到、缺勤/请假、出勤率显示正确 | □ |
| DASH-02 | 有进行中签到任务时查看学生首页 | 显示课程、教室、截止时间和快捷签到入口 | □ |
| DASH-03 | 学生完成签到后返回首页 | 最近考勤和出勤趋势更新 | □ |
| DASH-04 | 教师登录查看首页 | 今日课程、进行中签到、待审批、平均出勤率、异常学生正确 | □ |
| DASH-05 | 教师发起签到后返回首页 | 实时签到卡出现，应到人数和进度正确 | □ |
| DASH-06 | 审批请假后返回首页 | 待审批数量减少 | □ |
| DASH-07 | 点击首页当前签到课程 | 进入签到页且课程已自动选中 | □ |

## 十六、角色权限与数据隔离

| 编号 | 测试步骤 | 预期结果 | 状态 |
|---|---|---|---|
| SEC-01 | 学生尝试调用创建、编辑、停用课程功能 | 返回 403，数据不变化 | □ |
| SEC-02 | 学生尝试查看其他学生的用户详情 | 返回 403 | □ |
| SEC-03 | 学生尝试修改其他账号密码 | 返回 403 | □ |
| SEC-04 | 学生尝试修改他人考勤状态 | 返回 403 | □ |
| SEC-05 | 教师尝试创建、编辑或停用任意课程 | 返回 403 | □ |
| SEC-06 | 教师尝试审批另一教师课程请假 | 返回 403 | □ |
| SEC-07 | 普通小组成员尝试添加或移除成员 | 返回 403 | □ |
| SEC-08 | 组长尝试为非本组学生签到 | 返回 403 | □ |
| SEC-09 | 不携带令牌访问受保护接口 | 返回未认证结果，不泄露数据 | □ |
| SEC-10 | 使用伪造、过期或已退出的令牌 | 请求被拒绝 | □ |
| SEC-11 | 输入非法考勤状态值 | 返回 400，不写入非法状态 | □ |
| SEC-12 | 传入不存在的课程、用户、考勤或请假编号 | 返回明确 404/业务提示，不返回服务器堆栈 | □ |
| SEC-13 | 检查响应和日志 | 不返回密码、JWT 密钥、人脸特征或服务器敏感路径 | □ |

## 十七、并发、边界和恢复能力

| 编号 | 测试步骤 | 预期结果 | 状态 |
|---|---|---|---|
| EDGE-01 | 两个窗口同时为同一学生提交同一课程签到 | 最终只有一条记录 | □ |
| EDGE-02 | 两个窗口同时审批同一请假 | 只有一次审批成功，状态一致 | □ |
| EDGE-03 | 两个窗口同时发起同课程同日签到 | 只有一个任务成功创建 | □ |
| EDGE-04 | 签到截止瞬间提交 | 结果以服务器时间为准，不因客户端时间产生矛盾 | □ |
| EDGE-05 | 修改电脑本地时间后查看任务 | 截止时间与状态仍由服务端控制 | □ |
| EDGE-06 | 网络短暂中断后恢复并重试查询 | 页面可恢复，不需要清库或重启 | □ |
| EDGE-07 | 人脸检测过程中关闭摄像头或离开页面 | 摄像头资源释放，无持续占用 | □ |
| EDGE-08 | 连续多次进入并退出人脸页面 | 不出现黑屏、多摄像头流或内存持续增长 | □ |
| EDGE-09 | 中文姓名、课程名、教室和请假原因 | 页面与 Excel 均无乱码 | □ |
| EDGE-10 | 空数据、单条数据和超过一页数据 | 页面都能正确展示 | □ |

## 十八、Android App 专项

| 编号 | 测试步骤 | 预期结果 | 状态 |
|---|---|---|---|
| APP-01 | 安装 `人脸考勤-Android-湖南大学主题版-20260909.apk` | 安装成功，应用名为“湖大人脸考勤” | □ |
| APP-02 | 首次启动 App | 启动底色、状态栏和登录页采用湖南大学主题 | □ |
| APP-03 | 在服务器设置中填写电脑局域网地址 | 保存成功；地址为 `http://电脑IP:8080/api` | □ |
| APP-04 | 手机与电脑处于同一 Wi-Fi 后登录 | 登录成功并加载首页 | □ |
| APP-05 | 填写 `127.0.0.1` 作为服务器地址 | 无法连接；更换为电脑局域网 IP 后恢复 | □ |
| APP-06 | 首次打开人脸页面并授予摄像头权限 | 能显示前置摄像头画面 | □ |
| APP-07 | 拒绝摄像头权限 | 给出明确提示，不闪退 | □ |
| APP-08 | 完成注册、个人签到和小组签到 | 与网页版结果一致 | □ |
| APP-09 | 使用系统返回键 | 优先返回上一页；首页再次返回时最小化应用 | □ |
| APP-10 | App 切到后台再恢复 | 登录状态和当前页面合理保留，摄像头无异常占用 | □ |
| APP-11 | 弱网下登录和签到 | 有加载状态与错误提示，不重复提交 | □ |
| APP-12 | 检查常见全面屏和底部手势区域 | 顶部状态栏、底部导航不被安全区遮挡 | □ |

## 十九、管理员信息管理专项

管理员前端仅保留“人员信息”和“课程信息”，不承担考勤、统计及请假工作。

| 编号 | 测试步骤 | 预期结果 | 状态 |
|---|---|---|---|
| ADM-01 | 管理员打开人员信息，并按角色或关键字筛选 | 返回正确用户和总数，不包含管理员、密码或人脸特征 | □ |
| ADM-02 | 管理员新增、查看和编辑学生或教师 | 保存成功；学生学号必填，教师可不填学号 | □ |
| ADM-03 | 管理员禁用临时测试账号 | 账号不能继续正常使用 | □ |
| ADM-04 | 管理员重新启用临时测试账号 | 账号恢复登录 | □ |
| ADM-05 | 管理员删除临时测试账号 | 删除成功，后续查询不存在 | □ |
| ADM-06 | 教师或学生调用管理员用户接口 | 返回 403 | □ |

## 二十、测试收尾与通过标准

### 收尾清理

- [ ] 删除测试期间注册的人脸照片与特征。
- [ ] 删除或停用专门创建的测试课程。
- [ ] 清理临时学生账号、小组和测试请假数据。
- [ ] 将被临时修改的课程星期、时段、班级恢复。
- [ ] 保留失败用例的截图、日志时间点和复现步骤。
- [ ] 确认分享包中不包含 `uploads`、`face_features.json`、本机数据库或真实人脸数据。

### 发布通过标准

- [ ] 所有 P0 主流程通过：登录、人员与课程管理、发起签到、快速/活体签到、考勤修正、请假审批、统计导出。
- [ ] 不存在越权读取或修改他人数据的问题。
- [ ] 活体模式下静态照片不能通过；两种模式下错误身份均不能签到。
- [ ] 无重复签到、重复签到任务或重复审批数据。
- [ ] Web 与 Android 均无阻断使用的布局、摄像头或网络问题。
- [ ] 前端生产构建、Android APK 构建、Java 测试和 Python 测试全部通过。

## 二十一、缺陷记录模板

| 字段 | 内容 |
|---|---|
| 缺陷编号 | BUG-YYYYMMDD-001 |
| 对应用例 | 例如 SIGN-12 |
| 严重程度 | P0 阻断 / P1 严重 / P2 一般 / P3 建议 |
| 测试账号 |  |
| 课程/日期 |  |
| 前置条件 |  |
| 复现步骤 | 1. 　2. 　3. |
| 实际结果 |  |
| 预期结果 |  |
| 截图或日志 |  |
| 是否稳定复现 | 是 / 否，复现次数： |
| 修复版本与复测结果 |  |
