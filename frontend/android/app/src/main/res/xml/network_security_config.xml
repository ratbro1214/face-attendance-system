<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Classroom LAN testing uses the computer's HTTP address. Production should use HTTPS. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
