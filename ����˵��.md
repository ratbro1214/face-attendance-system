# 人脸考勤系统——运行说明

压缩包内包含网页版完整源码、Android 工程及可直接安装的测试 APK。

## 一、只体验 Android App

安装根目录下的 `人脸考勤-Android-debug.apk`。

App 不是单机版，仍然需要一台电脑或云服务器运行后端与人脸识别服务。首次打开 App，在登录页展开“服务器设置（首次使用）”，填写：

```text
http://运行后端的电脑局域网IP:8080/api
```

例如电脑 IP 是 `192.168.1.8`，则填写 `http://192.168.1.8:8080/api`。手机和电脑需连接同一个 Wi-Fi。不要填写 `127.0.0.1` 或 `localhost`，因为它们在手机上指向手机自身。

## 二、在 Windows 上运行完整项目

需要安装：

- JDK 17
- Maven 3.9 或更高版本
- Node.js 18 或更高版本
- Python 3.12

### 1. 启动人脸识别服务

```powershell
cd face-recognition
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python app_opencv.py
```

成功后访问 `http://127.0.0.1:5000` 应能看到服务状态。

### 2. 启动 Java 后端

另开一个终端：

```powershell
cd backend
mvn spring-boot:run
```

分享包中同时附带了已编译的 `backend/attendance-system.jar`。如果只安装了 JDK 17、没有安装 Maven，也可以运行：

```powershell
cd backend
java -jar attendance-system.jar --spring.profiles.active=local
```

后端地址为 `http://127.0.0.1:8080/api`。默认使用项目内 H2 数据库，不需要安装 MySQL。

### 3. 启动网页前端

再开一个终端：

```powershell
cd frontend
npm install
npm run dev
```

浏览器访问 `http://127.0.0.1:5173`。

## 三、默认测试账号

| 角色 | 用户名 | 密码 |
|---|---|---|
| 管理员 | admin | admin123 |
| 教师 | teacher1 | password123 |
| 学生 | student1 | password123 |

## 四、重新生成 Android APK

```powershell
cd frontend
npm install
npm run android:apk
```

生成位置：`frontend/android/app/build/outputs/apk/debug/app-debug.apk`。

如果构建工具提示找不到 Android SDK，请安装 Android Studio，并在 `frontend/android/local.properties` 中配置本机 `sdk.dir`。该文件包含个人电脑路径，因此没有放入转发包。

## 五、注意事项

- 本压缩包不包含任何历史上传照片、人脸特征、本地数据库数据或账号私有数据。
- 新环境首次运行会自动创建测试数据库。
- 局域网给手机使用时，需要允许 Windows 防火墙访问 8080 和 5000 端口。
- 给多人长期使用时，需要把 Java 后端、Python 人脸服务和数据库部署到云服务器，并配置 HTTPS。
- 人脸信息属于敏感个人信息，实际使用前应取得用户授权，并提供删除入口和访问控制。

## 2026-09-05 升级说明

请阅读 `docs/功能修改与验收说明-20260905.md`。使用 MySQL 的旧环境须先执行新增迁移脚本；默认 H2 模式会自动补充表和字段。人脸服务现在需要 MediaPipe 和 `models/face_landmarker.task`，升级后重新安装依赖并重启服务。

## 2026-09-09 角色流程升级

- 管理员只查看正常课程；课程的创建、停用、恢复和删除改由任课老师完成。
- 老师可修改或取消当天签到，并按课程分配小组、指定组长。
- 学生可在“人脸注册”中首次录入；录入后自动锁定，重录须提交申请并由任课老师批准。
- MySQL 旧环境升级前请执行 `database/migration-20260909-role-workflows.sql`；默认 H2 环境启动新版后端时会自动升级。
